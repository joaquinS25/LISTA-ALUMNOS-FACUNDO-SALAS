import { PipeMayusculaPipe } from './pipe-mayuscula.pipe';

describe('PipeMayusculaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeMayusculaPipe();
    expect(pipe).toBeTruthy();
  });
});
